import styled from 'styled-components'

export default function Header() {
    return (
        <SectionContainer>
            <h1>Header</h1>
        </SectionContainer>
    )
}

const SectionContainer = styled.div`
  
`
